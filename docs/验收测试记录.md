# 验收测试记录（第 9 章验收场景 + 测试报告回归场景）

运行时间：2026-09-12；合计 27 项，通过 27 项，失败 0 项。

| # | 场景 | 检查方法 | 预期结果 | 结果 | 实际 |
|---|---|---|---|---|---|
| 1 | 终端注册 | 两个终端分别注册，断开其中一个再连接 | 鱼塘归属及连接数正确，另一个终端仍可上传 | 通过 | 注册后 {'TERM-A01': 'online', 'TERM-A02': 'online'}，断开 A02 后 {'TERM-A01': 'online', 'TERM-A02': 'offline'} |
| 2 | 环境数据上传 | 上传一条含鱼塘/设备/时间/单位的有效测量 | 两端可查到同一记录，显示来源及更新时间 | 通过 | 最新水温 25.7℃ @ 2026-09-12 23:51:41 来源=sim |
| 3 | 数据失效 | 提供超量程的必需输入 | 标明无效原因，不生成有效投喂建议 | 通过 | 超量程记录被标记 valid=False，原因：超量程（有效范围 0.0~40.0） |
| 4 | 日内数据整理 | 提供包含缺测和异常值的日内数据 | 保存原始曲线，正确标记异常，统计窗口与样本数可查 | 通过 | 窗口=2026-09-12 00:00 ~ 24:00，有效样本=2/2，缺测=0 |
| 5 | 温度参与计算 | 提供两组不同有效温度特征的输入 | 结果符合规则，能追溯特征窗口、原始输入和版本 | 通过 | 适温建议 37.398kg > 低温建议 26.178kg；规则=rule-v1.0 |
| 6 | 正常投喂 | 有权限用户确认建议，终端返回完整执行反馈 | 同一任务依次更新状态，保留建议量、确认量和反馈 | 通过 | 状态=done，建议=41.85，确认=41.85，实际=10.2，回执 3 条 |
| 7 | 重复提交 | 重复提交同一请求及重复发送同一回执 | 只对应一个任务和一次执行，不重复统计 | 通过 | 两次请求均返回 TK-0001；重复回执后实际量仍为 9.9 |
| 8 | 回执超时 | 任务下发后中断回执 | 显示待核查及原因，不自动再次投料 | 通过 | 状态=unknown，设备占用=True（未自动重发） |
| 9 | 核查与恢复 | 核实未执行的任务，登记结论及依据，再尝试新建任务 | 有权限者登记结论及依据；核查结束且设备满足恢复条件时解除占用 | 通过 | 结论=not_executed，任务状态=failed，设备占用=False |
| 10 | 迟到与重复回执 | 核查后补发原任务回执 | 回执关联原任务，冲突进入再次核对并保留证据 | 通过 | 迟到回执 1 条，冲突标记 1 条 |
| 11 | 实际量未知 | 设备返回完成但没有实际量 | 显示未获取，不以确认量冒充实测量 | 通过 | 实际量=None（未获取），确认量=41.85，未互相顶替 |
| 12 | 停止与卡料 | 模拟卡料反馈 | 按设备反馈更新状态，保留异常和已知执行量 | 通过 | 状态=failed，实际量=0.0，产生异常告警=True |
| 13 | 权限限制 | 使用无控制权限账户提交任务 | 后端拒绝执行并返回原因 | 通过 | 生成建议 HTTP 403（无权访问该鱼塘）；提交任务 HTTP 403（无权访问该鱼塘） |
| 14 | 跨塘越权 | 仅 A01 权限用户用 A02 建议下发任务/读建议/关告警/读日志 | 建议详情、任务创建、告警处理、日志查询均拒绝并返回原因 | 通过 | 读建议 403，跨塘下发 403，关告警 403，读日志 403（均应 403） |
| 15 | 执行中超时 | 终端 accept 后失联，触发超时巡检 | running 阶段超时同样转待核查并保留设备占用，不自动重发 | 通过 | 状态=unknown，占用=True，巡检 timed_out=1 |
| 16 | 乱序回执 | 先 finish/done 再补发 execute/running | 迟到过程回执留痕但不把已完成任务改回执行中 | 通过 | 状态=done（应保持 done），占用=False，回执 2 条留痕 |
| 17 | 取消建议 | 取消待确认建议后重复取消并用其下发任务 | 状态转 cancelled；重复取消被拒；已取消建议不能再下发任务 | 通过 | 取消后状态=cancelled，重复取消 HTTP 409，下发 HTTP 400（该建议已取消，不能执行） |
| 18 | 输入校验 | 未来采集时间/NaN/坏 days/不存在鱼塘/非法回执/负实测量 | 全部返回 400/404 并给出原因，任务状态不被非法回执改变 | 通过 | 未来时间 400，NaN 400，days 400/400，缺塘 404，非法状态 400，负实测量 400，任务保持 dispatched |
| 19 | 模型训练与测试 | 用 60 天称重/8 天投喂/24 小时水质训练三个模型并预测 | 输出目标/单位/时间范围/版本明确；未训练时明确报错，不返回伪预测 | 通过 | 训练 growth/feed/water 全成功=True；生长预测当前 380.0g、日增 5.533g、达标 76 天；投喂预测明日 34.03kg；未训练塘返回 reason=model_not_trained |
| 20 | 死鱼事件 | 登记两条疑似事件，分别确认-处理与标记误报 | 识别/登记结果为待确认事件，确认与处理留痕 | 通过 | 事件1 handled，事件2 false_alarm（确认前均不计入已确认数量） |
| 21 | 多塘比较 | 两个塘都有近期数据时查询 7 天窗口比较 | 每塘返回环境均值/投喂/告警/用药等可比指标 | 通过 | 返回 2 个塘；A01 水温均值=26.0 投喂=0kg；A02 水温均值=27.0 投喂=0kg |
| 22 | 计划与运营 | 建计划并完成；两次称重+投喂后查询饲料系数；登记用药与成本；导出记录 | 各接口返回完整且口径可解释，导出含时间/单位/来源 | 通过 | 计划完成=True，FCR=0.018（口径：2026-08-13 ~ 2026-09-12 称重区…），用药登记=True，导出 1 条含来源=True |
| 23 | FCR口径 | 两次称重间完成 100kg、之后失败 500kg，查 feed-stats 与排行榜 | 失败任务不计入用料；分子限称重区间；两页面 FCR 一致 | 通过 | feed-stats FCR=0.016（期望 0.016），用料=100.0kg，排行榜 A02 FCR=0.016 |
| 24 | 交流与排行榜 | 发帖回帖并关闭；A01 两次称重+投喂，A02 不给称重，查询排行 | 交流帖可发布/回复/关闭；FCR 可算的塘参与排名，数据不足明确不参与 | 通过 | 发帖回复关闭均成功=True；A01 FCR=0.014（用料 66.0kg / 增重 4860.0kg（08-23 ~ 09-12 称重区间））；A02 未参与原因：称重记录不足两次或缺少投魂数量，无法计算饲料系数 |
| 25 | 并发建议 | 8 线程并发 16 次生成建议 | 编号生成并发安全，全部 HTTP 200 | 通过 | 成功 16/16，失败状态码：无 |
| 26 | 视频与识别 | 上传合成相机帧（含亮色漂浮物）并查询相机与事件；再传图做 OCR | 帧被真实分析并生成待确认事件；相机转在线；OCR 依赖缺失时明确说明 | 通过 | 检出漂浮物=True 置信度=0.9 模型=cv-float-v1，相机在线=True，事件待确认=True，OCR 明确=True |
| 27 | 数据导入 | 导入 测试数据_2024-01-01_2024-12-31.csv 再重复导入一次（进程内直连同一 SQLite） | 首次写入并保留来源记录编号；重复导入不重复写入 | 通过 | 首次写入 1830 条，重复导入写入 0 条、跳过重复 366 条，记录数 1830->1830 |
